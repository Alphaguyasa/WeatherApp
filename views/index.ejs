<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Weather Forecast (Weatherbit API)</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/styles/style.css" />
  </head>
  <body>
    <div class="container py-5">
      <div class="text-center mb-4">
        <h1 class="fw-bold">Weather Forecast Lookup</h1>
        <p class="text-muted">
          Powered by <strong>Weatherbit API (3-Hourly Forecast)</strong>
        </p>
      </div>

      <div class="glass-card mb-4">
        <h4 class="section-title">Enter Latitude & Longitude</h4>
        <form id="weather-form" method="post" action="/get-weather">
          <div class="row g-3">
            <div class="col-md-6">
              <label for="lat" class="form-label">Latitude</label>
              <input
                type="number"
                step="any"
                class="form-control"
                id="lat"
                placeholder="35.5"
                required
                name="latitude"
              />
            </div>
            <div class="col-md-6">
              <label for="lon" class="form-label">Longitude</label>
              <input
                type="number"
                step="any"
                class="form-control"
                id="lon"
                placeholder="-78.5"
                required
                name="longitude"
              />
            </div>
          </div>
          <div class="text-center mt-4">
            <button type="submit" class="btn btn-primary px-4">
              Get Forecast
            </button>
          </div>
        </form>
      </div>

      <% if (locals.weatherData) { %>
      <div id="weather-result" class="glass-card text-center">
        <h3 id="result-location" class="fw-bold mb-2">
          Coordinates: <%= latitude %> , <%= longitude %>
        </h3>

        <p class="text-muted mb-1">
          City: <%= weatherData.city_name %> (<%= weatherData.country_code %>)
        </p>

        <img
          id="weather-icon"
          class="icon mb-2"
          src="https://www.weatherbit.io/static/img/icons/<%= weatherData.data[0].weather.icon %>.png"
          alt="Weather Icon"
        />

        <h2 id="temp" class="mb-0"><%= weatherData.data[0].temp %> °F</h2>
        <p id="weather-main" class="fw-semibold">
          <%= weatherData.data[0].weather.description %>
        </p>

        <hr />

        <div class="row mt-3 text-start">
          <div class="col-md-6">
            <h5 class="section-title">Atmospheric Details</h5>
            <ul class="list-group list-group-flush">
              <li class="list-group-item">
                Pressure: <%= weatherData.data[0].pres %> hPa
              </li>
              <li class="list-group-item">
                Humidity: <%= weatherData.data[0].rh %> %
              </li>
              <li class="list-group-item">
                Dew Point: <%= weatherData.data[0].dewpt %> °F
              </li>
              <li class="list-group-item">
                Clouds: <%= weatherData.data[0].clouds %> %
              </li>
              <li class="list-group-item">
                Visibility: <%= weatherData.data[0].vis %> km
              </li>
            </ul>
          </div>

          <div class="col-md-6">
            <h5 class="section-title">Wind & Timing</h5>
            <ul class="list-group list-group-flush">
              <li class="list-group-item">
                Wind Speed: <%= weatherData.data[0].wind_spd %> m/s
              </li>
              <li class="list-group-item">
                Wind Direction: <%= weatherData.data[0].wind_dir %> °
              </li>
              <li class="list-group-item">
                Forecast Time: <%= weatherData.data[0].timestamp_local %>
              </li>
            </ul>
          </div>
        </div>
      </div>
      <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <!-- <script src="../show.js"></script> -->
  </body>
</html>
